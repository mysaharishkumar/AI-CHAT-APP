{
  "name": "ai-chat-app-email-service",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny standalone service that sends OTP emails via Nodemailer, called by the Python backend over HTTP.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13"
  }
}
